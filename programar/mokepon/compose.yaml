services:
  js-app:
    build:
      context: .
    container_name: js-app
    restart: unless-stopped
    init: true
    # env_file: ./.env  # Uncomment if .env file exists
    # No ports exposed since none are specified in Dockerfile or documentation
    # No volumes needed as this is not a database or persistent service
    # Add ports below if your app listens on a specific port (e.g., 3000)
    # ports:
    #   - "3000:3000"

# No external services (databases, caches, etc.) detected in project files or Dockerfile
# No volumes section needed as there are no persistent data requirements
